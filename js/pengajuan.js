async function renderPengajuan(){showView("pengajuan");const v=document.getElementById("pengajuanView");v.innerHTML=`<div class="page-title"><div><h2>Pengajuan Saya</h2><span class="muted">Pantau semua pengajuan surat.</span></div><button class="primary-btn" onclick="route('chat')">+ Ajukan Surat</button></div><div class="panel table-wrap"><table><thead><tr><th>Tiket</th><th>Jenis</th><th>Tanggal</th><th>Status</th><th>Aksi</th></tr></thead><tbody id="pgBody"><tr><td colspan="5">Memuat...</td></tr></tbody></table></div>`;try{const r=await api("myPengajuan"),b=document.getElementById("pgBody");b.innerHTML=r.items.length?r.items.map(x=>`<tr><td><b>${esc(x.ticket_no)}</b></td><td>${esc(x.jenis_nama)}</td><td>${esc(x.tanggal_pengajuan)}</td><td><span class="badge">${esc(x.status)}</span></td><td><button class="ghost-btn" onclick="openPengajuan('${esc(x.pengajuan_id)}')">Detail</button></td></tr>`).join(""):`<tr><td colspan="5" class="empty">Belum ada pengajuan.</td></tr>`}catch(e){document.getElementById("pgBody").innerHTML=`<tr><td colspan="5">${esc(e.message)}</td></tr>`}}
async function openPengajuan(id){try{const r=await api("getPengajuan",{pengajuan_id:id}),x=r.item;route("chat");setTimeout(()=>{const b=document.getElementById("chatBody");b.innerHTML=`<div class="bubble bot"><b>${esc(x.ticket_no)}</b><br>${esc(x.jenis_nama)}<br><span class="badge">${esc(x.status)}</span></div><div class="panel"><b>Timeline</b><div class="stepper">${(x.timeline||[]).map(t=>`<span class="step ${t.done?"done":""}">${esc(t.label)}</span>`).join("")}</div><p>${esc(x.catatan||"Tidak ada catatan.")}</p></div>`},100)}catch(e){toast(e.message,true)}}
