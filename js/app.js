document.addEventListener("DOMContentLoaded",async()=>{document.getElementById("loginForm").onsubmit=async e=>{e.preventDefault();try{const r=await login(document.getElementById("loginIdentity").value.trim(),document.getElementById("loginPassword").value);enterApp(r.user)}catch(x){document.getElementById("loginMsg").textContent=x.message}};document.getElementById("logoutBtn").onclick=logout;document.querySelectorAll(".nav-item").forEach(n=>n.onclick=()=>route(n.dataset.view));const ok=await restoreSession();if(ok)enterApp(CURRENT_USER)});
async function enterApp(u){document.getElementById("loginView").classList.add("hidden");document.getElementById("appView").classList.remove("hidden");document.getElementById("logoutBtn").classList.remove("hidden");document.getElementById("userCard").innerHTML=`<b>${esc(u.nama)}</b><small>${esc(u.role)} · ${esc(u.nim||"")}</small>`;const can=["SUPERADMIN","ADMIN","KAPRODI","WADEK","DEKAN"].includes(u.role);document.getElementById("workflowNav").classList.toggle("hidden",!can);await loadLetterTypes();renderDashboard();loadNotifications()}
function route(v){document.querySelectorAll(".nav-item").forEach(n=>n.classList.toggle("active",n.dataset.view===v));showView(v);if(v==="dashboard")renderDashboard();if(v==="chat")renderChat();if(v==="pengajuan")renderPengajuan();if(v==="workflow")renderWorkflow()}
function showView(v){document.querySelectorAll(".view").forEach(x=>x.classList.add("hidden"));document.getElementById(v+"View").classList.remove("hidden")}
function renderDashboard(){showView("dashboard");const v=document.getElementById("dashboardView");v.innerHTML=`<div class="hero"><h2>Halo, ${esc(CURRENT_USER?.nama||"")} 👋</h2><p>Urus surat kampus tanpa ribet.</p></div><div class="page-title"><h2>Layanan cepat</h2></div><div class="service-grid">${LETTERS.map(x=>`<button class="service" onclick="route('chat')"><span class="ico">📄</span><b>${esc(x.nama_surat)}</b><small>Ajukan secara digital</small></button>`).join("")}</div>`}
function toast(t,error=false){const x=document.getElementById("toast");x.textContent=t;x.style.background=error?"#8e3028":"#163d34";x.classList.add("show");setTimeout(()=>x.classList.remove("show"),3200)}
